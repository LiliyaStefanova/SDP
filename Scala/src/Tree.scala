/**
 * Created by liliya on 10/01/2015.
 * Program to manipulate simple arithmetic expressions containing
 * vars (x, y), constants(numbers) and + arithmetic operation
 * nodes are operations and leaves const or var
 */
abstract class Tree {

  /*
  Case class specifics:
  new word not required to crate an instance Sum(l, r)
  getter functions auto defined for constructor params
  default def of equals and hashCode, working on structure of the instances
  default def for toString is provided, prints the value in a
  "source form"
  instances of hte classes can be decomposed through pattern matching
   */


}
